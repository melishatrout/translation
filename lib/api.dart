import 'dart:io';
/**
 * Need methods that will do the following:
 * 1. Connects to the API
 * 2. Translate the text
 * 3. Returns the translated text
 */

/// Api class connect to the google translation api and returns the translated text
class Api {
  final HttpClient _httpClient = HttpClient();
}
